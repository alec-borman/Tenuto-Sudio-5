<scratchpad>
Error/Requirement Analysis: The AST linearization is currently using floatToFraction, which introduces IEEE 754 floating-point inaccuracies. Also, PlaybackOrchestrator is doing its own regex parsing instead of consuming the AST events.

Constraint Alignment:
- MUST remove floatToFraction.
- MUST use pure rational fractions (numerator, denominator) and cross-multiplication for time logic.
- MUST reduce fractions via GCD.
- MUST accept compiledEvents (any[]) in PlaybackOrchestrator.compileAndPlay instead of a string.
- MUST calculate ticks as num * 1920 / den. If there's a remainder, warn and skip playback.
- MUST update SplitWorkspace to pass compiledEvents to PlaybackOrchestrator.
- MUST update playback_orchestrator.test.ts.
- MUST add new test rational_linearization.test.ts.

Execution Plan:
1. src/parser/IncrementalCompiler.ts: Update to accumulate rational times, use gcd.
2. src/audio/PlaybackOrchestrator.ts: Refactor compileAndPlay, eliminate ASTSerializer parsing.
3. src/components/SplitWorkspace.tsx: Change parameter of compileAndPlay.
4. tests/playback_orchestrator.test.ts: Apply TDB content.
5. tests/rational_linearization.test.ts: Apply TDB content.
6. Verify everything passes via npx vitest run.

Completeness Check:
- Verified that all exports will hold true logic.
- GCD handles cross-multiplication.
- PPQ math respects exact integer division requirements.

Tool Verification:
I will use edit_file, create_file and shell_exec to execute these actions.
</scratchpad>
